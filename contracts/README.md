# Vault contract (Foundry)

Build step 5 — deliberately last: the interface is dictated by the running mint (built against a fake vault first), not guessed. Design constraints live in [spec/05-vault.md](../spec/05-vault.md).
